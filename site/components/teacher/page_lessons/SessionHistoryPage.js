import styles from './SessionHistoryPage.module.css';

export default function SessionHistoryPage(props){
    return(
        <p>hello from session history page lessonId: {props.lessonId}</p>
    );
}