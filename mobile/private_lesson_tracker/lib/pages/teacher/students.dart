import 'package:flutter/material.dart';

class TeacherStudents extends StatelessWidget{
  const TeacherStudents({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('hello from teacher TeacherStudents');
  }
}