import 'package:flutter/material.dart';

class TeacherSchedule extends StatelessWidget{
  const TeacherSchedule({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('hello from teacher Schedule');
  }
}