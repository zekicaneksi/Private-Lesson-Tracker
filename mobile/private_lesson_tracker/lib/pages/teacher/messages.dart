import 'package:flutter/material.dart';

class TeacherMessages extends StatelessWidget{
  const TeacherMessages({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('hello from teacher TeacherMessages');
  }
}