import 'package:flutter/material.dart';

class GuardianStudents extends StatelessWidget{
  const GuardianStudents({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('hello from guardian GuardianStudents');
  }
}