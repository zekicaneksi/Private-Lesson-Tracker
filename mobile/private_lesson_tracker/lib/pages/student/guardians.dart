import 'package:flutter/material.dart';

class StudentGuardians extends StatelessWidget{
  const StudentGuardians({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('hello from student StudentGuardians');
  }
}