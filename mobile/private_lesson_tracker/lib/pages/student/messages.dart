import 'package:flutter/material.dart';

class StudentMessages extends StatelessWidget{
  const StudentMessages({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('hello from student StudentMessages');
  }
}