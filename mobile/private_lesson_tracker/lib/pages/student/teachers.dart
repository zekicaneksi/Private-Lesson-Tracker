import 'package:flutter/material.dart';

class StudentTeachers extends StatelessWidget{
  const StudentTeachers({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('hello from student StudentTeachers');
  }
}